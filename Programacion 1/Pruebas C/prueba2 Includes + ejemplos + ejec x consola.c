// Entrada/salida estándar: printf, scanf, etc.
#include <stdio.h>      

// Utilidades generales: malloc, free, atoi, exit, etc.
#include <stdlib.h>     

// Funciones para trabajar con cadenas: strcpy, strlen, strcmp, etc.
#include <string.h>     

// Tipos booleanos: bool, true, false
#include <stdbool.h>    

// Tipos enteros de tamaño fijo: int8_t, uint32_t, etc.
#include <stdint.h>     

// Funciones matemáticas: sqrt, pow, sin, etc.
#include <math.h>       

// Manejo de tiempo y fecha: time, clock, etc.
#include <time.h>       

// Funciones para analizar caracteres: isdigit, toupper, etc.
#include <ctype.h>      

// API de Windows: funciones gráficas, ventanas, MessageBox, etc.
#include <windows.h>    

int main() {
    // Mostrar un mensaje por consola (stdio.h)
    printf("Hola mundo\n"); 

    /* Copiar una cadena (string.h)
    char nombre[50];
    strcpy(nombre, "Facundo"); 

    // Usar booleanos (stdbool.h)
    bool activo = true; 

    // Usar entero de 32 bits (stdint.h)
    int32_t edad = 30;  

    // Calcular raíz cuadrada (math.h)
    double raiz = sqrt(64.0); 

    // Verificar si un carácter es un dígito (ctype.h)
    if (isdigit('9')) {
        printf("Es un digito\n");
    }

    // Obtener hora actual (time.h)
    time_t ahora = time(NULL); 

    // Mostrar ventana emergente (windows.h)
    MessageBox(NULL, "Ventana simple", "Desde C", MB_OK); 

    return 0;*/
}
